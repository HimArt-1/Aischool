# 🧪 اختبار التثبيت والتشغيل

## قائمة التحقق السريعة

استخدم هذا الملف للتأكد من أن كل شيء يعمل بشكل صحيح.

---

## 1️⃣ التحقق من بنية الملفات

### المجلدات الرئيسية:
```
✅ /workspace/src/
✅ /workspace/src/AdversarialWargameModule/
```

### ملفات الوحدة الرئيسية (7 ملفات):
```
✅ src/AdversarialWargameModule/types.ts
✅ src/AdversarialWargameModule/DigitalTwinMap.ts
✅ src/AdversarialWargameModule/RedAI.ts
✅ src/AdversarialWargameModule/BlueAI.ts
✅ src/AdversarialWargameModule/SimulationEngine.ts
✅ src/AdversarialWargameModule/WargameDashboard.tsx
✅ src/AdversarialWargameModule/index.ts
```

### ملفات التطبيق:
```
✅ src/App.tsx
✅ src/main.tsx
✅ src/vite-env.d.ts
```

### ملفات الإعداد:
```
✅ package.json
✅ tsconfig.json
✅ tsconfig.node.json
✅ vite.config.ts
✅ eslintrc.cjs
✅ index.html
✅ .gitignore
```

### ملفات الوثائق:
```
✅ README.md
✅ ARCHITECTURE.md
✅ DEMO_GUIDE.md
✅ QUICKSTART.md
✅ PROJECT_SUMMARY.md
✅ INSTALLATION_TEST.md (هذا الملف)
```

**المجموع:** ~20 ملف رئيسي ✅

---

## 2️⃣ التحقق من المتطلبات

### تحقق من Node.js:
```bash
node --version
# يجب أن يكون >= 18.0.0
```

### تحقق من npm:
```bash
npm --version
# يجب أن يكون >= 9.0.0
```

---

## 3️⃣ اختبار التثبيت

### الخطوة 1: التثبيت
```bash
npm install
```

**المخرجات المتوقعة:**
- تثبيت ~10-15 حزمة
- لا أخطاء في console
- إنشاء مجلد `node_modules/`
- إنشاء `package-lock.json`

**إذا ظهرت أخطاء:**
```bash
# حذف المجلدات القديمة
rm -rf node_modules package-lock.json

# إعادة التثبيت
npm install
```

---

## 4️⃣ اختبار البناء

### التحقق من TypeScript:
```bash
npx tsc --noEmit
```

**المخرجات المتوقعة:**
- لا أخطاء
- إكمال بدون مشاكل

**إذا ظهرت أخطاء:**
- تحقق من إصدار TypeScript: `npx tsc --version`
- يجب أن يكون >= 5.2.0

---

## 5️⃣ اختبار Linting

```bash
npm run lint
```

**المخرجات المتوقعة:**
- لا أخطاء
- ربما تحذيرات قليلة (مقبولة)

---

## 6️⃣ اختبار التشغيل

### بدء الخادم:
```bash
npm run dev
```

**المخرجات المتوقعة:**
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
  ➜  press h to show help
```

### التحقق من المتصفح:
1. افتح `http://localhost:3000`
2. يجب أن ترى شاشة ترحيبية بعنوان:
   ```
   🎯 SCAR-EYE Adversarial Wargame Simulation Module
   ```
3. اضغط **"🚀 ابدأ المحاكاة"**
4. يجب أن ترى:
   - خريطة مع مباني (رمادي)
   - كاميرات (أخضر)
   - أزرار التحكم (تشغيل، إيقاف، إعادة تعيين)

---

## 7️⃣ اختبار الوظائف

### اختبار التشغيل:
1. اضغط زر **"▶️ تشغيل"**
2. يجب أن ترى:
   - عداد الجولات يزيد
   - خطوط حمراء تظهر على الخريطة (مسار Red-AI)
   - نقاط حمراء وخضراء/زرقاء (المواقع)
   - الإحصائيات تتحدث في اللوحة الجانبية

### اختبار الإيقاف:
1. اضغط **"⏸️ إيقاف"**
2. يجب أن يتوقف العداد
3. الخريطة تبقى كما هي

### اختبار إعادة التعيين:
1. اضغط **"🔄 إعادة تعيين"**
2. يجب أن:
   - عداد الجولات يعود إلى 0
   - الخريطة تمسح
   - الإحصائيات تختفي

---

## 8️⃣ اختبار الأداء

### تشغيل 50 جولة:
1. ابدأ المحاكاة
2. دعها تعمل لـ 50 جولة
3. لاحظ:
   - **FPS يجب أن يبقى > 30**
   - لا تأخير ملحوظ
   - الذاكرة لا تزيد بشكل مفرط

### في DevTools (F12):
```
Performance Tab:
- CPU usage < 50%
- Memory < 200MB
- No memory leaks
```

---

## 9️⃣ اختبار التوافق

### المتصفحات:
- ✅ Chrome (موصى به)
- ✅ Firefox
- ✅ Edge
- ✅ Safari (macOS)

### الأجهزة:
- ✅ Desktop (موصى به)
- ✅ Laptop
- ⚠️ Tablet (قد يكون بطيئاً)
- ❌ Mobile (غير موصى به - الشاشة صغيرة)

### الدقة:
- ✅ 1920x1080 (موصى به)
- ✅ 1366x768 (مقبول)
- ⚠️ 1280x720 (قد تظهر scrollbars)

---

## 🔟 اختبار البناء للإنتاج

```bash
npm run build
```

**المخرجات المتوقعة:**
```
vite v5.x.x building for production...
✓ xxx modules transformed.
dist/index.html                  x.xx kB
dist/assets/index-xxxxxx.css     x.xx kB
dist/assets/index-xxxxxx.js    xxx.xx kB
✓ built in xxxs
```

### معاينة البناء:
```bash
npm run preview
```

يجب أن يعمل تماماً مثل `npm run dev`

---

## ✅ قائمة التحقق النهائية

قبل العرض أو التسليم، تأكد من:

- [ ] جميع الملفات موجودة
- [ ] `npm install` يعمل بدون أخطاء
- [ ] `npm run lint` لا يظهر أخطاء خطيرة
- [ ] `npm run dev` يشغل التطبيق
- [ ] التطبيق يفتح في المتصفح
- [ ] شاشة الترحيب تظهر بشكل صحيح
- [ ] زر "ابدأ المحاكاة" يعمل
- [ ] زر "تشغيل" يبدأ المحاكاة
- [ ] الخريطة تظهر بوضوح
- [ ] الإحصائيات تتحدث
- [ ] زر "إيقاف" يوقف المحاكاة
- [ ] زر "إعادة تعيين" يعيد كل شيء
- [ ] الأداء جيد (> 30 FPS)
- [ ] لا أخطاء في Console
- [ ] `npm run build` يعمل

**إذا كانت جميع النقاط ✅، فالمشروع جاهز 100%!**

---

## 🐛 استكشاف الأخطاء

### المشكلة: "Cannot find module"
```bash
# حل:
npm install
```

### المشكلة: "Port 3000 already in use"
```bash
# حل: استخدام منفذ آخر
npm run dev -- --port 3001
```

### المشكلة: الشاشة بيضاء فارغة
```bash
# تحقق من Console (F12) للأخطاء
# عادة يكون خطأ في التثبيت
rm -rf node_modules package-lock.json
npm install
```

### المشكلة: الأداء بطيء
```bash
# حلول:
# 1. أغلق التطبيقات الأخرى
# 2. استخدم Chrome (الأسرع)
# 3. قلل عدد الجولات في الكود
```

### المشكلة: TypeScript errors
```bash
# تأكد من الإصدار الصحيح
npx tsc --version
# يجب أن يكون >= 5.2.0

# إذا كان أقدم:
npm install -D typescript@latest
```

---

## 📞 الدعم

إذا واجهت مشاكل:

1. ✅ راجع **QUICKSTART.md**
2. ✅ راجع **ARCHITECTURE.md**
3. ✅ تحقق من Console (F12)
4. ✅ جرب `npm install` مرة أخرى
5. ✅ جرب متصفح مختلف

---

## 📊 معايير النجاح

التطبيق يعتبر ناجحاً إذا:

✅ **الوظائف:**
- المحاكاة تعمل لـ 200 جولة بدون أخطاء
- الإحصائيات تتحدث بشكل صحيح
- الواجهة تستجيب للأزرار

✅ **الأداء:**
- FPS > 30 معظم الوقت
- الذاكرة < 300MB
- لا تجميد أو تأخير

✅ **البصريات:**
- الخريطة واضحة
- الألوان تظهر بشكل صحيح
- الرسوم المتحركة سلسة

✅ **الاستقرار:**
- لا أخطاء في Console
- يمكن إعادة التعيين والتشغيل عدة مرات
- لا memory leaks

---

**🎉 إذا نجحت في كل الاختبارات، مبروك! المشروع جاهز للاستخدام والعرض!**

---

*آخر تحديث: ديسمبر 2025*
*النسخة: 1.0.0*
