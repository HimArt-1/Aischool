# 🚀 دليل البدء السريع (Quick Start Guide)

## متطلبات التشغيل

- **Node.js**: الإصدار 18 أو أحدث
- **npm**: مدير الحزم
- **متصفح حديث**: Chrome, Firefox, Safari, or Edge

## التثبيت والتشغيل

### الخطوة 1: تثبيت المتطلبات

```bash
npm install
```

### الخطوة 2: تشغيل التطبيق

```bash
npm run dev
```

سيفتح التطبيق تلقائياً في المتصفح على `http://localhost:3000`

### الخطوة 3: بناء للإنتاج (اختياري)

```bash
npm run build
```

الملفات المبنية ستكون في مجلد `dist/`

## الاستخدام الأساسي

### 1. شاشة الترحيب
عند فتح التطبيق، ستظهر شاشة ترحيبية تشرح:
- المكونات الخمسة للوحدة
- الملاحظات الأمنية
- كيفية الاستخدام

اضغط **"🚀 ابدأ المحاكاة"** للمتابعة.

### 2. لوحة التحكم الرئيسية

#### الخريطة (يسار الشاشة):
- المباني: مستطيلات رمادية
- الطرق: شبكة داكنة
- الكاميرات: نقاط خضراء مع دوائر الرؤية
- نقاط الإخفاء: دوائر صفراء شفافة

#### اللوحة الجانبية (يمين الشاشة):
- **الإحصائيات**: معدل الكشف، الثقة، الأخطاء
- **تفاصيل آخر جولة**: الاستراتيجية والتحليل
- **المفتاح**: شرح الألوان والرموز

### 3. أزرار التحكم

- **▶️ تشغيل**: بدء المحاكاة
- **⏸️ إيقاف**: إيقاف مؤقت
- **🔄 إعادة تعيين**: البدء من جديد
- **ℹ️** (أسفل يمين): إعادة فتح شاشة المعلومات

## فهم المحاكاة

### ما يحدث في كل جولة:

1. **Red-AI** (المهاجم):
   - يختار استراتيجية إخفاء
   - يحدد موقع الإخفاء (نقطة حمراء)
   - ينشئ مسار حركة (خط أحمر)
   - يطبق تمويه طيفي

2. **Blue-AI** (SCAR-EYE):
   - يحلل البصمات الطيفية
   - يبحث عن شذوذات
   - يتنبأ بموقع الإخفاء (نقطة خضراء/زرقاء)
   - يرفع الإضاءة في المناطق المشبوهة

3. **التقييم**:
   - يتم حساب المسافة بين الموقع الفعلي والمتوقع
   - تحديد نجاح/فشل الكشف
   - كلا الوكيلين يتعلمان من النتيجة

### مؤشرات الأداء:

- **معدل الكشف**: نسبة الكشف الناجح من إجمالي الجولات
- **متوسط الثقة**: ثقة Blue-AI في توقعاته
- **خطأ المسافة**: متوسط المسافة بين الموقع الفعلي والمتوقع
- **النتائج**: Red-AI Wins vs Blue-AI Wins

## استكشاف الأخطاء

### المشكلة: التطبيق لا يبدأ

```bash
# تأكد من تثبيت المتطلبات
npm install

# تأكد من إصدار Node.js
node --version  # يجب أن يكون 18 أو أحدث
```

### المشكلة: التطبيق بطيء

- أغلق التطبيقات الأخرى
- جرب متصفح آخر
- قلل عدد الجولات في الكود:

```typescript
// في src/App.tsx
const simulation = createSimulation({
  totalRounds: 100, // بدلاً من 200
  speedMultiplier: 2, // زيادة السرعة
});
```

### المشكلة: الألوان لا تظهر بشكل صحيح

- تأكد من أن المتصفح يدعم Canvas API
- جرب تحديث المتصفح
- تأكد من عدم وجود إضافات تحجب JavaScript

## التخصيص

### تغيير عدد الجولات:

في `src/App.tsx`:

```typescript
const simulation = createSimulation({
  totalRounds: 300, // العدد المطلوب
});
```

### تغيير حجم الخريطة:

في `src/App.tsx`:

```typescript
const simulation = createSimulation({
  mapConfig: {
    width: 1200,  // العرض
    height: 900,  // الارتفاع
    gridSize: 60,
    // ... باقي الإعدادات
  },
});
```

### تغيير سرعة المحاكاة:

في `src/AdversarialWargameModule/SimulationEngine.ts`، سطر ~197:

```typescript
const delay = Math.max(10, 50 / this.config.speedMultiplier);
// غير 50 إلى 100 للإبطاء، أو 25 للإسراع
```

## استخدام متقدم

### استدعاء الوحدة برمجياً:

```typescript
import {
  createSimulation,
  type RoundResult,
} from './AdversarialWargameModule';

// إنشاء محاكاة
const engine = createSimulation({ totalRounds: 100 });

// الاستماع للأحداث
engine.on('roundCompleted', (result: RoundResult) => {
  console.log(`الجولة ${result.roundNumber}: كشف = ${result.actualDetection}`);
});

// تشغيل تلقائي
engine.runAutomatic((round, total) => {
  console.log(`التقدم: ${round}/${total}`);
}).then(stats => {
  console.log('اكتملت المحاكاة:', stats);
});
```

### تصدير النتائج:

```typescript
// في نهاية المحاكاة
const resultsJSON = engine.exportResults();
console.log(resultsJSON);

// حفظ في ملف (في بيئة Node.js)
import fs from 'fs';
fs.writeFileSync('results.json', resultsJSON);
```

## الأوامر المتاحة

```bash
# تطوير
npm run dev

# بناء
npm run build

# معاينة البناء
npm run preview

# فحص الأكواد (إذا كان eslint مكوناً)
npm run lint
```

## الدعم والمساعدة

### الموارد:
- **README.md**: معلومات عامة عن المشروع
- **ARCHITECTURE.md**: شرح تفصيلي للبنية المعمارية
- **DEMO_GUIDE.md**: دليل العرض في الهاكثون

### الأسئلة الشائعة:

**س: هل يمكن استخدام بيانات حقيقية؟**
ج: لا، الوحدة مصممة للمحاكاة فقط. استخدام بيانات حقيقية يتطلب تعديلات أمنية كبيرة.

**س: هل يمكن إضافة استراتيجيات جديدة؟**
ج: نعم، في `src/AdversarialWargameModule/RedAI.ts`، أضف استراتيجية جديدة في `selectStrategy()` و `selectHideLocation()`.

**س: هل يمكن تغيير الألوان؟**
ج: نعم، في `src/AdversarialWargameModule/WargameDashboard.tsx`، قسم `styles` في النهاية.

## النصائح والحيل

### للعروض التوضيحية:
1. ابدأ بـ 50 جولة للاختبار السريع
2. استخدم وضع ملء الشاشة (F11)
3. أغلق التطبيقات الأخرى لتحسين الأداء

### للتطوير:
1. استخدم TypeScript لتجنب الأخطاء
2. اختبر كل مكون على حدة
3. اقرأ التعليقات في الكود

### للتعلم:
1. ابدأ بفهم `types.ts`
2. تتبع تدفق البيانات من `SimulationEngine`
3. جرب تعديل الاستراتيجيات والمعاملات

## الخطوات التالية

بعد تشغيل المحاكاة بنجاح:

1. ✅ اقرأ **ARCHITECTURE.md** لفهم البنية
2. ✅ جهز العرض باستخدام **DEMO_GUIDE.md**
3. ✅ جرب تخصيص الإعدادات
4. ✅ استكشف الكود المصدري
5. ✅ شارك مع الفريق!

---

**مبروك! 🎉 أنت الآن جاهز لاستخدام وحدة SCAR-EYE Adversarial Wargame Simulation!**

لأي أسئلة أو مشاكل، راجع **ARCHITECTURE.md** أو **DEMO_GUIDE.md**.
