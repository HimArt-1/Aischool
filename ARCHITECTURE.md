# 🏗️ بنية الوحدة المعمارية

## نظرة عامة على البنية

وحدة `AdversarialWargameModule` مبنية بطريقة معيارية قابلة للتوسع، حيث كل مكون مستقل ويتفاعل مع المكونات الأخرى عبر واجهات محددة.

```
AdversarialWargameModule/
├── types.ts                 # تعريفات الأنواع المشتركة
├── DigitalTwinMap.ts        # محاكاة الخريطة
├── RedAI.ts                 # الذكاء المهاجم
├── BlueAI.ts                # الذكاء الدفاعي
├── SimulationEngine.ts      # محرك المحاكاة
├── WargameDashboard.tsx     # واجهة العرض
└── index.ts                 # نقطة التصدير الرئيسية
```

## 1. DigitalTwinMap (التوأم الرقمي)

### المسؤوليات:
- توليد خريطة حضرية مصغرة مع مباني وطرق
- إدارة نقاط الإخفاء المحتملة ومواقع الكاميرات
- محاكاة طبقات طيفية متعددة (مرئي، أشعة تحت حمراء، حراري، طيفي فائق)
- إدارة خريطة الإضاءة التكيفية
- التحقق من خطوط الرؤية والعوائق

### التقنيات المستخدمة:
- محاكاة شبكية Grid-based simulation
- نظام بصمات طيفية Spectral Signature System
- خريطة إضاءة ديناميكية Dynamic Illuminance Map
- حسابات Line-of-Sight

### المخرجات الرئيسية:
```typescript
interface MapConfig {
  width: number;
  height: number;
  gridSize: number;
  buildings: MapObject[];
  roads: MapObject[];
  hideSpots: MapObject[];
  cameras: MapObject[];
  ambientLight: number;
}
```

## 2. Red-AI Agent (المهاجم)

### المسؤوليات:
- اختيار استراتيجيات إخفاء متنوعة
- إنشاء ملفات تمويه طيفية
- توليد مسارات حركة ذكية
- التعلم من المحاولات السابقة

### الاستراتيجيات المتاحة:
1. **darkZone**: البحث عن أحلك منطقة
2. **cameraBlind**: إيجاد نقاط عمياء للكاميرات
3. **crowdedArea**: الاختباء في المناطق المزدحمة
4. **spectralCamouflage**: استخدام تمويه طيفي متقدم
5. **randomNoise**: حركة عشوائية (استراتيجية تشويش)

### نظام التعلم:
- يحتفظ بأوزان للاستراتيجيات
- يعدل الأوزان بناءً على النجاح/الفشل
- معدل تعلم قابل للتخصيص (Learning Rate)

### المخرجات:
```typescript
interface RedAIOutput {
  hideLocation: Position;
  camouflageProfile: CamouflageProfile;
  path: Position[];
  confidence: number;
  strategyUsed: string;
}
```

## 3. Blue-AI Agent (SCAR-EYE الدفاعي)

### المسؤوليات:
- تحليل طيفي شامل للخريطة
- كشف الشذوذات والانحرافات
- التنبؤ بمواقع الإخفاء المحتملة
- التعلم من الأنماط السابقة
- تعديل الإضاءة بشكل تكيفي

### تقنيات الكشف:
1. **Spectral Analysis**: تحليل البصمات الطيفية
2. **Anomaly Detection**: كشف الانحرافات عن الخط الأساسي
3. **Pattern Recognition**: التعرف على الأنماط من الذاكرة
4. **Camera Coverage Analysis**: تحليل تغطية الكاميرات
5. **Illuminance Analysis**: تحليل مستويات الإضاءة

### نظام الذاكرة:
- يحفظ آخر 100 نمط
- يتعلم من النجاحات والإخفاقات
- يعدل عتبة الشك (Suspicion Threshold) ديناميكياً

### المخرجات:
```typescript
interface BlueAIOutput {
  detected: boolean;
  confidence: number;
  predictedLocation: Position;
  reasoning: string;
  illuminanceAdjustment: number;
  spectralAnalysis: {
    anomalyScore: number;
    suspiciousRegions: Position[];
  };
}
```

## 4. SimulationEngine (محرك المحاكاة)

### المسؤوليات:
- إدارة دورة حياة المحاكاة
- تنفيذ الجولات بين Red-AI و Blue-AI
- حساب النتائج وتقييم الكشف
- جمع الإحصائيات
- إدارة نظام الأحداث

### حالات المحاكاة:
- `idle`: جاهز للبدء
- `running`: قيد التشغيل
- `paused`: إيقاف مؤقت
- `completed`: اكتملت

### نظام الأحداث:
```typescript
engine.on('simulationStarted', (data) => { ... });
engine.on('roundCompleted', (result) => { ... });
engine.on('simulationCompleted', (stats) => { ... });
```

### معايير تقييم الكشف:
- المسافة بين الموقع الفعلي والمتوقع
- مستوى ثقة Blue-AI
- عوامل احتمالية بناءً على المسافة والثقة

## 5. WargameDashboard (واجهة العرض)

### المسؤوليات:
- رسم الخريطة الحية على Canvas
- عرض مسارات الحركة والتوقعات
- إظهار الإحصائيات في الوقت الفعلي
- توفير أدوات التحكم (تشغيل/إيقاف/إعادة تعيين)

### طبقات الرسم:
1. **Grid Layer**: شبكة الخلفية
2. **Roads Layer**: الطرق
3. **Buildings Layer**: المباني
4. **Hide Spots Layer**: نقاط الإخفاء
5. **Cameras Layer**: الكاميرات ومجالات الرؤية
6. **Movement Layer**: مسارات الحركة
7. **Prediction Layer**: التوقعات والنتائج

### عناصر الواجهة:
- خريطة تفاعلية (Canvas)
- لوحة إحصائيات
- عداد الجولات
- مؤشر معدل الكشف
- رسم بياني لاتجاه الثقة
- تفاصيل آخر جولة
- مفتاح توضيحي

## تدفق البيانات

```
1. SimulationEngine.executeRound()
   ↓
2. RedAI.executeHidingAttempt()
   → يختار موقع إخفاء واستراتيجية
   ↓
3. BlueAI.attemptDetection(redAIMove)
   → يحلل ويتنبأ بالموقع
   ↓
4. SimulationEngine.evaluateDetection()
   → يقيم نجاح الكشف
   ↓
5. RedAI.learn() & BlueAI.learn()
   → كلا الوكيلين يتعلمان من النتيجة
   ↓
6. engine.emit('roundCompleted', result)
   → إشعار الواجهة بالنتيجة
   ↓
7. WargameDashboard يحدث العرض
```

## اعتبارات الأداء

### التحسينات المطبقة:
- استخدام Grid-based sampling بدلاً من Pixel-by-pixel
- تحديد عدد العينات في التحليل الطيفي
- استخدام requestAnimationFrame للرسم
- تخزين مؤقت للحسابات الثقيلة
- تحديد حجم ذاكرة الأنماط (100 نمط كحد أقصى)

### قابلية التوسع:
- سهولة إضافة استراتيجيات جديدة لـ Red-AI
- إمكانية توسيع تقنيات الكشف في Blue-AI
- واجهات مفتوحة لإضافة طبقات طيفية جديدة
- نظام أحداث قابل للتوسع

## الأمان والقيود

### القيود الصارمة:
- ❌ لا اتصال بشبكات خارجية
- ❌ لا وصول لملفات النظام
- ❌ لا استخدام لبيانات حقيقية
- ✅ بيانات محاكاة فقط (Mock Data)
- ✅ للأغراض التعليمية فقط

### فصل المسؤوليات:
- الوحدة معزولة تماماً عن باقي النظام
- لا تعديلات على ملفات خارج الوحدة
- استخدام TypeScript للأمان النوعي
- واجهات صارمة بين المكونات

## الاستخدام المستقبلي

### إمكانيات التوسع:
1. إضافة AI agents جديدة (Green-AI, Yellow-AI, etc.)
2. توسيع أنواع الخرائط (ريفية، صناعية، إلخ)
3. إضافة أنماط طيفية أكثر تعقيداً
4. دمج نماذج ML حقيقية
5. إضافة وضع متعدد اللاعبين
6. تصدير البيانات للتحليل الإحصائي

### التكامل:
```typescript
// مثال على التكامل في مشروع أكبر
import { createSimulation, WargameDashboard } from './AdversarialWargameModule';

const simulation = createSimulation({
  totalRounds: 500,
  speedMultiplier: 2,
});

// استخدام في React
<WargameDashboard engine={simulation} autoPlay={true} />
```

## الخلاصة

الوحدة مصممة لتكون:
- ✅ معيارية وقابلة للصيانة
- ✅ قابلة للتوسع
- ✅ محاكاة تعليمية واقعية
- ✅ مذهلة بصرياً
- ✅ آمنة للعرض في الهاكثون
